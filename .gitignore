# log files
*.log

# swagger output
swagger.yaml

# conf files
conf.json
prod_conf.json
canary_conf.json

# Project binaries
bin/

# Local videos to serve
*.mp4
# ...except the landing page's self-hosted demo clip, which must ship.
!web/public/demo.mp4

# Editor cruft
*.swo
*.swp

# Python bytecode (codegen scripts under server/code_generation/)
__pycache__/
*.pyc

# Local DB dumps in the repo root (migrations live under server/)
/*.sql
.gstack/

# Web build output + the staged-build swap dirs (see build-web)
web/build/
web/build.next/
web/build.prev/

# Generated by `make landing-assets` from web/src/landing.scss and the
# @fontsource packages. Never hand-edit; never commit.
web/public/landing.css
web/public/fonts/

# OS files
.DS_Store

# Claude Code local-only config
.claude/settings.local.json
.claude/worktrees/
